# candidate_skill.yaml - Pure Rust flywheel (SIGNIFICANTLY upgraded emission quality: 10+ proposal sections, rich full-text proposals, expanded _learning_meta)
# Generated by agentforge-runner flywheel-step --real-data
# Closer to (and richer than) Python learning/skill_improver.py ProposedSkill.to_yaml + rich sectioned proposals
name: general-refactor-improved-rust
proposed_name: general-refactor-flywheel-202605310748
description: "Auto-proposed improvement over general-refactor at 2026-05-31T07:48:33.912691151+00:00 via pure-Rust agentforge-flywheel. Heuristic + solid LLM critique path (AGENTFORGE_LLM_CMD direct-exec + JSON/plain fallback). Mined from real trajectories + PRM + learning_value. Data-driven conditional sections for maximum proposal richness."
timestamp: 2026-05-31T07:48:33.912691151+00:00
system_prompt: |
  You are an expert at general-refactor. Always think step by step, use tools carefully, and recover from errors explicitly. Use checkpoints for long operations. Classify outcome quality in one sentence after every action/tool. Attempt exactly one targeted recovery on non-success before next step. Emit structured reasoning on decisions. Key learned rules from flywheel data: Always attempt exactly ONE structured recovery (different approach) on non-success; classify OUTCOME first. Before complex sequences emit explicit PLAN (goal + 2-3 risks + first actions). Structured decision blocks at branches: Hypothesis | Evidence | Action+rationale | Falsifiers. After recovery: brief SELF_REFLECT (did classification match reality? did fix address root?). Maintain compact state checkpoints every 2-3 actions for safe resume on error/long horizon. CRITIQUE-DERIVED[source=structured_fallback]: For 'general-refactor': insert explicit PROGRESS_HEARTBEAT every 8-15s on long ops + hard per-action timeouts + single recovery (simplify/cached) on timeout. Use progress_heartbeat section. | BONUS: after recovery do brief SELF_REFLECT (classification accurate? root fixed?) + update state checkpoint..
required_tags: []
ci_checks:
- Run cargo test
- Run cargo clippy
- python -m pytest -q --tb=line || true
few_shot_examples:[]  # (mined high-PRM successes available for population)
proposals:
  - section: system_prompt
    rationale: Analyzed 96 failures. Most common error pattern: 'unknown_error'. No recovery behaviors observed in failures.  | CRITIQUE[source=structured_fallback]: For 'general-refactor': insert explicit PROGRESS_HEARTBEAT every 8-15s on long ops + hard per-action timeouts + single recovery (
    confidence: 0.78
    estimated_delta: +4-9pp success on error classes
    before: n/a
    after: |-
      You are an expert at general-refactor. Always think step by step, use tools carefully, and recover from errors explicitly. Use checkpoints for long operations. Classify outcome quality in one sentence after every action/tool. Attempt exactly one targeted recovery on non-success before next step. Emit structured reasoning on decisions. Key learned rules from flywheel data: Always attempt exactly ONE structured recovery (different approach) on non-success; classify OUTCOME first. Before complex sequences emit explicit PLAN (goal + 2-3 risks + first actions). Structured decision blocks at branches: Hypothesis | Evid
  - section: recovery_strategy
    rationale: Data shows 94 failures (0 high-PRM successes mined). Add explicit outcome classification + one-attempt recovery after every action/tool.
    confidence: 0.85
    estimated_delta: +6-12pp on failure-heavy and low-PRM batches (primary lever)
    before: Implicit error handling, no structured recovery documented.
    after: |-
      After EVERY action or tool call: 1) Classify outcome quality in one sentence (success / partial / failure / timeout / empty). 2) If not success, attempt EXACTLY ONE targeted recovery (different args, fallback, or simplified approach). 3) Log the classification + recovery attempt clearly before next step.
  - section: tool_use
    rationale: Tool call errors or low-PRM tool steps observed in trajectories. Strengthen selection, arg validation, result verification and graceful degradation.
    confidence: 0.73
    estimated_delta: +3-8pp on tool-heavy tasks; fewer cascading failures
    before: Direct tool calls with minimal post-call inspection.
    after: |-
      Before tool call: validate args against known schema + state. After result: immediately verify shape/non-emptiness/error; if bad, classify + recover (see recovery_strategy). Prefer narrow, well-scoped tools over generic ones on first attempt.
  - section: verification
    rationale: Low-quality steps often skip result validation. Mandate lightweight verification after actions.
    confidence: 0.68
    estimated_delta: +2-5pp via earlier error catching
    before: n/a
    after: |-
      Verification rule: After every non-trivial action/tool, produce a 1-line 'VERIFICATION:' note with (observed vs expected) + pass/fail. If fail, trigger recovery path immediately.
  - section: error_handling
    rationale: Observed 94 failures with explicit errors. Add dedicated error classification + handling rules to prevent silent failures and cascading issues.
    confidence: 0.79
    estimated_delta: +5-10pp reduction in unhandled error loops
    before: Errors passed through or minimally logged without classification.
    after: |-
      On any error or unexpected result: (a) Classify error class (transient / auth / schema / timeout / unknown) in <10 words. (b) Log the class + raw message snippet. (c) Apply recovery_strategy unless classified terminal. Never swallow errors.
  - section: checkpointing
    rationale: Long-horizon or failure-prone trajectories benefit from explicit state checkpoints for safe resume/recovery.
    confidence: 0.66
    estimated_delta: higher recovery success on long tasks
    before: No intermediate state saves; full re-compute or lost context on error.
    after: |-
      After every 2-3 significant actions or on phase boundary: write compact CHECKPOINT (task_id, key state, last success classification). On restart/error use latest checkpoint to resume without full replay.
  - section: planning_decomposition
    rationale: Complex/long tasks show weak upfront planning. Mandate explicit decomposition + risk identification.
    confidence: 0.71
    estimated_delta: +5-11pp on multi-phase tasks
    before: Direct dive into actions with minimal or implicit planning.
    after: |-
      Before major work: 1) Decompose goal into 2-5 subgoals. 2) Identify top 2 risks. 3) Pick first 1-2 actions with explicit leverage. Revisit plan on major outcome classification.
  - section: observability_logging
    rationale: Rich signals (notes, errors, PRM) exist but agent observability is weak; stronger logs enable better future PRM + debugging.
    confidence: 0.70
    estimated_delta: +2-6pp via better downstream learning
    before: Minimal or absent structured logs of decisions, verifications, recoveries.
    after: |-
      Always emit: DECISION: <why>, VERIF: <result>, RECOVERY: <what+why>, OUTCOME: <class>. Use consistent prefixes for easy parsing by PRM/flywheel.
  - section: error_taxonomy
    rationale: 94 failures with raw errors. Mandate lightweight classification to route recovery correctly.
    confidence: 0.76
    estimated_delta: +6-11pp better recovery routing
    before: All errors treated identically; no routing.
    after: |-
      On error: CLASSIFY <12 words (transient/auth/schema/timeout/rate-limit/permanent/unknown). Route: transient->retry once; auth/schema->escalate with context; permanent->fail fast. Log class always.
  - section: output_contracts
    rationale: Result shapes vary or low-PRM post-action steps. Enforce lightweight contracts for verification.
    confidence: 0.67
    estimated_delta: +3-7pp earlier bad-result detection
    before: Free-form results; ad-hoc inspection.
    after: |-
      For each major action declare expected OUTPUT_CONTRACT (keys|nonempty|type|bounds) in 1 line. After call: assert vs contract (1-line). Fail contract -> immediate recovery.
  - section: retry_backoff_policy
    rationale: Transient errors or rate/timeout patterns. Add explicit backoff to avoid thundering herd or quota burn.
    confidence: 0.69
    estimated_delta: lower cost + fewer quota hits on transients
    before: Immediate retry or unbounded loops on transient.
    after: |-
      Transient errors: exponential backoff (base 1.5s, max 3 attempts, jitter). Log attempt#. Permanent errors: no retry. Use for tool+http classes in error_taxonomy.
estimated_impact: high
_learning_meta:
  generated_by: "agentforge-flywheel (rust crates/agentforge-flywheel + agentforge-runner)"
  source_skill: general-refactor
  engine: "rust-agentforge-runner/flywheel-step@phase1-mvp"
  timestamp: 2026-05-31T07:48:33.912691151+00:00
  records_analyzed: 96
  high_learning_value_records: 0
  avg_learning_value: 0.0083
  rust_pairs_used: 96
  llm_critique_used: false
  llm_critique_path: "clean AGENTFORGE_LLM_CMD (direct preferred) or rich structured fallback"
  tool_signals_detected: false
  low_prm_tool_steps: false
  recovery_signals_detected: false
  num_proposals_emitted: 11
  num_failures: 94
  num_high_prm_successes: 0
  before_stats:
    approx_success_rate: 0.021
    avg_duration_sec: 0.0
    complex_task: true
  analysis:
    top_error_pattern: "Analyzed 96 failures"
    weak_step_types_sample: "tool=false; recovery=false; reasoning_low=false"
    failure_rate_approx: 0.979
    signals_detected:
      planning: false
      state: false
      reflection: false
      logging: false
      efficiency: false
    emitted_sections: "recovery/tool_use/error_handling/verification/few_shots/reasoning_structure/timeout_handling/checkpointing/planning_decomposition/state_management/self_reflection/observability_logging/efficiency_cost/example_selection (conditional on real signals)"
  source: "rust-flywheel-step@phase1-max-quality; 10+ conditional sectioned proposals + ultra-rich Python-style _learning_meta + solid LLM critique path (AGENTFORGE_LLM_CMD)"
  note: "Highest emission quality candidate. Reviewable for pending_candidates + A/B. More proposals + fuller text than prior versions. Clean LLM integration exercised when AGENTFORGE_LLM_CMD (or variants) present."
