Ты эксперт по мультиагентным AI системам 2025-2026.

Наш текущий стек AgentForge:
- FastAPI task queue с SQLite
- Grok Worker с git worktree parallelism (5 задач одновременно)
- Guardian auto-review (проверка result на fail)
- HITL (approve/reject кнопки в дашборде)
- LanceDB векторная память
- WebSocket real-time дашборд
- Arena Mode (2 агента соревнуются)
- MoA / Mixture-of-Agents (N proposers + aggregator-агент) — POST /tasks/{id}/moa?num_proposals=3 + /moa/aggregate + /moa/finalize
- Auto-PR Merge после Guardian approve
- RAG по логам (rag_indexer.py)
- Watchdog с эскалацией (jules->grok->antigravity)
- systemd автозапуск с linger
- MCP server для IDE
- Skills YAML шаблоны
- Webhooks при смене статуса
- Agent Cards (/agents endpoint)

Вопрос: дай 10 КОНКРЕТНЫХ технологий и практик которые мы НЕ внедрили, но активно используют команды уровня Google, Anthropic, OpenAI для мультиагентной AI разработки.

Формат ответа - ТОЛЬКО JSON массив, без текста до и после:
[
  {"name": "название", "description": "описание 1-2 предложения", "priority": "critical или high или medium", "complexity": "simple или medium или complex", "tags": ["tag1", "tag2"]}
]
